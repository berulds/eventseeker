class ApiServices
  require URI
  require JSON

  # will need to require some stuff, start with the above.

  # build API fetch in here
  # def initialize(query)
  #   @query = query
  # end

  def self.call_google_events_api(query)
    # must return what we want to obatin from the API
  end
end

ApiServices.call_google_events_api(query)
