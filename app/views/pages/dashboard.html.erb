<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-12 col-lg-6">

      <div>
        <h1> Hi, I'm a dashboard. I need content. </h1>
      </div>

      <div class="mt-5">
        <p>These are just links so we can access all our current pages</p>
        <ul>
          <li><%= link_to "View all my events", events_path, class: "dash-link" %></li>
          <li><%= link_to "Create an event", new_event_path, class: "dash-link" %></li>
          <li><%= link_to "Create an itinerary", new_itinerary_path, class: "dash-link" %></li>
          <li><%= link_to "View Your Bookmarks", event_bookmarks_path(current_user), class: "dash-link" %></li>
        </ul>
      </div>

      <div class="dashboard-itinerary-card">
        <%= link_to "View All My Itineraries", itineraries_path, class: "btn btn-primary event-show-more" %>
          <% @itinerary.order(:start_time).each do |itin| %>
            <% if itin.user_id == current_user.id %>
              <div class="d-flex justify-content-between">
                <div>
                  <%= link_to itin.name, itinerary_path(itin), class: "btn btn-warning" %>
                </div>
                <div class="d-flex align-items-center">
                  <p><%= itin.start_time.strftime('%dth %b') %> - <%= itin.end_time.strftime('%dth %b') %></p>
                </div>
              </div>
            <% end %>
          <% end %>
      </div>

    </div>
  </div>
</div>
